import { environment } from '../../environments/environment';

export const Env = {
  endpoint: environment.ENDPOINT,
  projectID: environment.PROJECT,
  databaseID: environment.DATABASE_ID,
  projectsID: environment.PROJECTS_COLLECTION_ID,
};
